<div class="row">
  <div class="col-xs-12">
    <% if logged_in? %>
      <%= bootstrap_form_for(@article) do |f| %>
        <% if @article.errors.any? %>
          <div id="error_explanation">
            <h2><%= pluralize(@article.errors.count, "error") %> prohibited this post from being saved:</h2>

            <ul>
            <% @article.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
            </ul>
          </div>
        <% end %>
        
        <div class="field">
          <%= f.text_field :url %>
        </div>
        <div class="field">
          <%= f.text_field :text %>
        </div>
        <div class="actions">
          <%= f.submit %>
        </div>
      <% end %>

    <% else %>
      <p>You must be signed in to save a article.</p>
    <% end %>
  </div>
</div>