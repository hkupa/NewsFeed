<%= form_for(@articles_tag) do |f| %>
  <% if @articles_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@articles_tag.errors.count, "error") %> prohibited this articles_tag from being saved:</h2>

      <ul>
      <% @articles_tag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
